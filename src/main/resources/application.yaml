server:
  port: 8086
spring:
  application:
    name: nihongo-user-service
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:180.93.115.154:9092}
  datasource:
    url: jdbc:mysql://onehost-amdcloudhn022602.000nethost.com:3306/tsezwyvdhosting_nihongo?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: tsezwyvdhosting_nihongo_app
    password: thuThuy@1
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      max-lifetime: 45000
      idle-timeout: 30000
      connection-timeout: 30000
      validation-timeout: 5000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

gemini:
  api-key: ${GEMINI_API_KEY}
  base-url: https://generativelanguage.googleapis.com
  model: gemini-3.1-flash-lite

jwt:
  secret: "U7tjRTsx0baZLsRhHGcc0FA/B7MJ65IfQ0jZHvUyYrE="
# Tài khoản nhận tiền hiển thị trên trang ví; cấu hình bằng biến môi trường.
wallet:
  bank-name: ${WALLET_BANK_NAME:}
  bank-account: ${WALLET_BANK_ACCOUNT:}
  bank-account-name: ${WALLET_BANK_ACCOUNT_NAME:}

  events:
    enabled: ${WALLET_EVENTS_ENABLED:true}
    topic: ${WALLET_EVENTS_TOPIC:wallet.events.v1}
    partitions: ${WALLET_EVENTS_PARTITIONS:3}
    replicas: ${WALLET_EVENTS_REPLICAS:1}
  instance-id: ${WALLET_INSTANCE_ID:${random.uuid}}
  websocket:
    allowed-origins: ${FRONTEND_ORIGIN:http://localhost:5173}

